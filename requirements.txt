# Core data handling
numpy==2.5.0
pandas==3.0.3

# TF-IDF / cosine similarity for career-narrative matching (see src/redrob_ranker/narrative.py)
scikit-learn==1.9.0
scipy==1.18.0

# scikit-learn transitive deps, pinned for reproducibility
joblib==1.5.3
threadpoolctl==3.6.0

# Sandbox demo app (Stage 9)
streamlit==1.58.0

# submission_metadata.yaml scaffolding / config loading
PyYAML==6.0.3
